# Loan Performance Intelligence Engine -- Intain Campus FinTech Challenge.
#
# Lower bounds are the minimum the code needs; the comment after each line is
# the version this pipeline was developed and verified against, so a run that
# behaves differently can be bisected against a known-good set.
#
#   make setup     creates .venv and installs these
#   python main.py runs every phase end to end

# --- Core data handling ---
pandas>=2.2                # verified 2.3.3
numpy>=1.26                # verified 2.3.5

# --- Phase 1: data intelligence and profiling (Task 1) ---
scipy>=1.12                # verified 1.16.3  -- KS test, chi-square, two-proportion test
tabulate>=0.9              # verified 0.10.0  -- DataFrame.to_markdown() in every report
markdown>=3.5              # verified 3.10.3  -- markdown -> HTML for the reports

# --- Phases 2-3: feature engineering and predictive modelling (Task 2) ---
scikit-learn>=1.4          # verified 1.9.0
lightgbm>=4.3              # verified 4.7.0   -- needs an OpenMP runtime; `brew install libomp` on macOS
xgboost>=2.0               # verified 3.2.0   -- alternative backend, same OpenMP requirement
joblib>=1.3                # verified 1.5.3   -- model persistence

# --- Phase 4: survival / competing-risk modelling (Task 3) ---
lifelines>=0.28            # verified 0.30.3  -- Kaplan-Meier, Nelson-Aalen, Cox with delayed entry
scikit-survival>=0.22      # verified 0.28.0  -- IPCW Brier / integrated Brier score

# --- Phase 5: anomaly detection (Task 4) ---
# Isolation Forest comes from scikit-learn; pyod is here for the ECOD and
# autoencoder detectors the anomaly module can be extended with.
pyod>=1.1                  # verified 3.6.5

# --- Phase 7: explainability and responsible AI (Task 6) ---
shap>=0.45                 # verified 0.51.0  -- TreeExplainer, tree_path_dependent

# --- Phase 8: LLM reviewer copilot (Task 7) ---
# Any OpenAI-compatible provider works; the client detects which from the key
# prefix in .env (gsk_ Groq, xai- xAI, sk- OpenAI).
openai>=1.30               # verified 3.6.0
python-dotenv>=1.0         # verified 1.2.3   -- loads the key from .env, never hardcoded
tenacity>=8.2              # verified 9.1.4   -- backoff on rate limits and 5xx

# --- Charts ---
matplotlib>=3.8            # verified 3.10.0

# --- Interactive dashboard (streamlit run app.py) ---
streamlit>=1.40            # verified 1.62.0

# --- Dev / quality ---
pytest>=8.0                # verified 9.1.1

# Deliberately absent, having been considered and rejected:
#   imbalanced-learn -- SMOTE was rejected for Task 2. A synthetic minority row
#     carries an interpolated rolling history belonging to no real loan.
#   missingno         -- superseded by src/profiling/figures.py, which renders
#     the missingness views straight into the report.
#   optuna, mlflow    -- no hyperparameter search or experiment tracking is in
#     the pipeline. Add them back when something actually imports them.
#   seaborn, jupyter  -- unused; figures are built directly on matplotlib.
