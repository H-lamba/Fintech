# --- Core data handling ---
pandas>=2.2
numpy>=1.26

# --- Phase 1: Data Intelligence and Profiling (Task 1) ---
scipy>=1.12              # KS test, chi-square, Cramer's V
tabulate>=0.9            # DataFrame.to_markdown() in the report builders
markdown>=3.5            # markdown -> HTML for the reports

# --- Phases 2-3: Feature Engineering and Predictive Modeling (Task 2) ---
scikit-learn>=1.4
lightgbm>=4.3            # needs an OpenMP runtime: `brew install libomp` on macOS
xgboost>=2.0             # alternative backend; same OpenMP requirement
joblib>=1.3              # model persistence

# --- Phase 4: Survival / Competing-Risk Modeling (Task 3) ---
lifelines>=0.28          # Kaplan-Meier, Nelson-Aalen, Cox PH with delayed entry
scikit-survival>=0.22    # IPCW Brier / integrated Brier score

# --- Charts ---
matplotlib>=3.8

# --- Phase 5: Anomaly Detection (Task 4) -- not yet built ---
pyod>=1.1                # Isolation Forest, ECOD

# --- Phase 7: Explainability (Task 6) -- not yet built ---
shap>=0.45

# --- Phase 8: LLM Reviewer Copilot (Task 7) -- xAI Grok ---
# Grok exposes an OpenAI-compatible endpoint, so the official openai client
# works by pointing base_url at https://api.x.ai/v1
openai>=1.30
python-dotenv>=1.0       # load XAI_API_KEY from .env, never hardcoded
tenacity>=8.2            # retry/backoff around LLM calls

# --- Dev / quality ---
pytest>=8.0

# Deliberately absent, having been considered and rejected:
#   imbalanced-learn -- SMOTE was rejected for Task 2. A synthetic minority row
#     carries an interpolated rolling history belonging to no real loan.
#   missingno         -- superseded by src/profiling/figures.py, which renders
#     the missingness views straight into the report.
#   optuna, mlflow    -- no hyperparameter search or experiment tracking is in
#     the pipeline yet. Add them back when something actually imports them.
#   seaborn, jupyter  -- unused; figures are built directly on matplotlib.
